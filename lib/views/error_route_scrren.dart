import 'package:flutter/material.dart';

class ErrorRouteScrren extends StatelessWidget {
  const ErrorRouteScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: double.infinity,
        height: double.infinity,
        alignment: Alignment.center,
        child: Text('Error Route Screen'),
      ),
    );
  }
}
